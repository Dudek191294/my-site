# Copy to .env.local for local overrides. Do not put production secrets here.

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=change_me
APP_SHARE_DIR=var/share
###< symfony/framework-bundle ###

###> symfony/routing ###
DEFAULT_URI=http://localhost:8085
###< symfony/routing ###

# Docker Compose (optional local DB — app does not require Doctrine yet)
POSTGRES_PASSWORD=changeme
